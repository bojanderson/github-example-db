SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO



CREATE VIEW [dbo].[vw_CUST_WITH_OUTSTANDING] AS 

	SELECT	CUST_CODE, CUST_NAME, OUTSTANDING_AMT
	FROM	dbo.CUSTOMER
	WHERE	OUTSTANDING_AMT > 3000

GO
